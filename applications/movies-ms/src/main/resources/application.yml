server.port: 8081

spring:
  application:
    name: movies-ms

  jpa:
    generate-ddl: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect

  datasource:
    url: jdbc:mysql://localhost:3306/movies?useSSL=false&user=root&password=password